download slick.zip from slick.ninjacave.com and unzip it into this folder.

move all of native files (.dll, .so, .dylib) to a native directory inside that folder.

ex:
    mkdir slick/native
    mv slick/*.{dll,so,dylib} slick/native

